import { AppShell } from "@/components/app/app-shell";

export const metadata = {
  title: "Route Builder — RouteStory",
  description: "Build and animate your travel route",
};

export default function AppPage() {
  return <AppShell />;
}
